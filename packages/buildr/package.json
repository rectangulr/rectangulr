{
  "name": "@rectangulr/buildr",
  "version": "0.0.3",
  "type": "module",
  "description": "Buildr is an async build graph library",
  "main": "dist/graph.js",
  "module": "dist/graph.js",
  "typings": "dist/graph.d.ts",
  "scripts": {
    "build": "tsup graph.ts --dts --format esm",
    "dev": "tsup graph.ts --dts --format esm --watch",
    "test": "echo \"Error: no test specified\""
  },
  "keywords": [],
  "license": "MIT",
  "dependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.2.0"
  },
  "devDependencies": {
    "publish-if-not-exists": "^1.1.0",
    "tsx": "^4.19.1",
    "zx": "^8.1.9"
  }
}